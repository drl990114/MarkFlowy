{
  "name": "tauri-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter=@linebyline/editor dev & pnpm --filter=@linebyline/linebyline dev",
    "build": "pnpm --filter=@linebyline/editor build",
    "test": "jest",
    "preview": "vite preview",
    "tauri:build": "tauri build",
    "tauri:dev": "tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0",
    "typescript": "^5.0.4"
  }
}
