import styled from 'styled-components'

export { default } from '../site/Header'
export type { SiteHeaderProps as NavbarProps } from '../site/Header'

export const Brand = styled.span`
  color: var(--ink);
  font-family: var(--sans);
  font-size: 1.125rem;
  font-weight: 700;
`
