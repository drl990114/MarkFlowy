// Keep keyboard focus visible for both text and icon-only controls without shifting layout.
export const focusFeedback =
  'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-control-focus'

// Filled, small controls need an outer indicator so their checked state stays legible.
export const compactControlFocus =
  'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-control-focus'
