{
  "name": "@markflowy/types",
  "version": "0.0.3",
  "types": "dist-types/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "delete": "rimraf node_modules",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "@markflowy/theme": "workspace:*",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
